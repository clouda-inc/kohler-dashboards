mutation CreateOrderIfNotExists($orderData: OrderDataInput) {
  createOrderIfNotExists(orderData: $orderData) {
    id
    soldToId
    userId
    sapOrderNumber
    poNumber
    shipToAddress
    skuNumber
    createdDate
    brand
  }
}
